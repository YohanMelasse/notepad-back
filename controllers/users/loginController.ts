// On cherche un utilisateur 
// SELECT * FROM USER WHERE id = $id;
// On attend le mot de passe hashé
// puis on le compare au mot de passe de l'utilisateur.
// Si aucun utilisateur n'a été trouvé alors,
// on renvoi une erreur 404 (notFound);
// si la réponse est ok, alors on renvoi la réponse au format json suivi du message.

